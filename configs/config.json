{
    "dataset": "MNIST",
    "model": "siamese",
    "batch_size": 64,
    "epochs": 10,
    "lr": 0.001,
    "embedding_dim": 128,
    "margin": 1.0,
    "optimizer": "adam",
    "device": "cuda",
    "save_path": "models/"
}